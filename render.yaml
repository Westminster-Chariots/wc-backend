services:
  - type: web
    name: wc-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && node create-version.js
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: NODE_ENV
        value: production
